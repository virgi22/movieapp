name: welcome_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.6  # Versión de `http`
  crypto: ^3.0.0
  firebase_core: ^3.7.0  # Dependencia de firebase_core
  firebase_auth: ^5.3.2  # Dependencia de firebase_auth
  cloud_firestore: ^5.0.0  # Dependencia de Firestore

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/movie.jpg  # Imágen de fondo
    - assets/images/matrix.jpg  # Nueva imagen para "The Matrix"
    - assets/images/batman.jpg  # Nueva imagen para "The Matrix"
    - assets/images/venom.jpg  # Nueva imagen para "The Matrix"
 
